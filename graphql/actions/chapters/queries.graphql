query getTutorialIndexes {
  chapters {
    data {
      ...ChapterIndex
    }
  }
}

query getChapterById($id: ID!) {
  chapter(id: $id) {
    data {
      id
      attributes {
        ...ChapterEntry
      }
    }
  }
}
